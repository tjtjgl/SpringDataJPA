spring:
  datasource:
    url: jdbc:mysql://localhost:3306/data_jpa?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: secui00@!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  #org.hibernate.type: trace
